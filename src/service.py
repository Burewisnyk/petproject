from dto_models import Pet, Owner
from pet_db import (add_pet, add_owner, get_pet_by_id, get_owner_by_id,
                    clear_db, add_pets, add_owners, get_all_pets, 
                    get_all_owners)

def get_pet_by_id(pet_id: int) -> Pet | None:
    pass
